<?php
// Настройки: разрешаем использовать ID сессии в URL, если куки недоступны
@ini_set("session.use_cookies", "0"); // Для теста можно принудительно отключить куки (или оставьте "1" для работы в обоих режимах)
@ini_set("session.use_only_cookies", "0");
@ini_set("session.use_trans_sid", "1");

// Запускаем сессию (это заменит ручную работу с setcookie)
session_start();

// --- Логика СЧЕТЧИКА ---
if (!isset($_SESSION['visits_count'])) {
    $_SESSION['visits_count'] = 0;
}
$_SESSION['visits_count']++; // Увеличиваем счетчик
$visits_count = $_SESSION['visits_count'];


// --- Логика ВРЕМЕНИ ---
$last_visit = '';
// Сначала считываем старое время (если есть), чтобы показать пользователю
if (isset($_SESSION['last_visit_time'])) {
    $last_visit = $_SESSION['last_visit_time'];
}

// Записываем текущее время как "последнее" для СЛЕДУЮЩЕГО захода
$_SESSION['last_visit_time'] = date('d-m-Y H:i:s');

?>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Последний визит (Сессии)</title>
</head>
<body>

<h1>Последний визит</h1>

<?php
if ($visits_count === 1) {
    echo "<p>👋 Добро пожаловать!</p>";
} else {
    echo "<p>Вы зашли на страницу {$visits_count} раз.</p>";

    if (!empty($last_visit)) {
        echo "<p>Последнее посещение: {$last_visit}</p>";
    } else {
        echo "<p>Это ваш второй визит, но время первого не было зафиксировано.</p>";
    }
}
?>

<hr>
</body>
</html>